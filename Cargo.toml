[package]
name = "glyph-game"
version = "0.1.0"
edition = "2021"

[dependencies]
macroquad = "0.4"

[dev-dependencies]
quickcheck = "1"
quickcheck_macros = "1"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
